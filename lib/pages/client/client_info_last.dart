import 'package:flutter/material.dart';

class ClientInfoLast extends StatelessWidget {
  const ClientInfoLast({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}